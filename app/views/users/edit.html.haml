%h1 Editing Account
.row
  .col-md-5
    = simple_form_for(@user) do |f|
      = f.error_notification
      = f.input :name
      = f.input :address
      = f.input :city
      = f.input :state
      = f.input :zip, :input_html => { :maxlength => 5, class: "zip-input" }
      = f.input :phone_number, label: "Phone Number", :input_html => { :maxlength => 10, class: "phone-input" }
      = f.button :submit
    = link_to 'Cancel', @user