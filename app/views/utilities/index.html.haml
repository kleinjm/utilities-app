%h1 Services
.well.well-sm
  Use this tab to add, edit, or remove a current utility service. You’ll need your account number for the utility service, so be sure to have your last paper bill nearby!
- if notice
  .alert.alert-success= notice
- if @utilities.blank?
  %em You currently have no services saved
- else
  %table.table.table-striped
    %tr
      %th Name
      %th Account Number
      %th
      %th
      %th For Demo
    - @utilities.each do |utility|
      %tr
        %td= utility.name
        %td= utility.account_number
        %td= link_to 'Edit', edit_utility_path(utility), class: "btn btn-default"
        %td= link_to 'Delete', utility, method: :delete, class: "btn btn-danger font-white", data: { confirm: 'Are you sure?' }
        - if utility.paid
          %td= link_to "Reset payment", home_pay_utility_path(id: utility.id, paid: false), method: :put
        - else
          %td
%br
= link_to 'Add New Service', new_utility_path, class: "btn btn-default margin-top-20"